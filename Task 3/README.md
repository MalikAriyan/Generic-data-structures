Inherit from Matrix2D.
Add the following mathematically critical operations:
	int sumOfPrime()
Find all prime numbers in matrix and return their sum.
	int determinant()
If matrix is square:
	Compute determinant (support at least 2×2 and 3×3).
	Return 0 if not square.
	Double spectralNorm()

Compute:
√sum of squares of all elements

That is:
√(∑_(i=1)^r▒∑_(j=1)^c▒a_ij^2 )

Menu-Based Program
Create a full menu:
1. Insert Element
2. Display Matrix
3. Sum of Prime Numbers
4. Determinant
5. Spectral Norm
0. Exit
